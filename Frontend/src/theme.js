import { createTheme } from '@material-ui/core';

export default createTheme({
    palette: {
        type: 'dark',
        primary: {
            main: '#1A1A1A',
        },
        secondary: {
            main: '#E3A019'
        },
    },
    sidebarWidth: 260,
    sidebarMobileHeight: 90,
});
